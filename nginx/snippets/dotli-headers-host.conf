# Headers for the host build (*.<domain>): not iframeable from third parties.
# nginx replaces, never merges, add_header sets across contexts — every
# location that needs these headers must include this snippet itself.
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-Content-Type-Options "nosniff" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header Access-Control-Allow-Origin "*" always;
add_header Access-Control-Allow-Methods "GET, PUT, DELETE, OPTIONS" always;
add_header Access-Control-Allow-Headers "Content-Type" always;
